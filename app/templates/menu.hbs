{{log model}}

<ul class="items-container">
	{{#each model as |item|}}
	<li class="menu-item">
		<h2>{{item.name}}</h2>
		<img src="{{item.image}}" alt="{{item.name}}">
		<p>
			{{#link-to "item" item.id}}Details{{/link-to}}
		</p>
	</li>
	{{/each}}
</ul>
{{outlet}}